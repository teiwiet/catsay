/// a favorite color
enum Color{
    Red,
    Green,
    Blue
}
/// a piece of mail
struct Mail{
    /// destination
    address : String,
}
/// add two numbers together
fn add(a:i32,b:i32) -> i32{
    /// lmao
    a + b
}
